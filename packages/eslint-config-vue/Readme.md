eslint config for vue

```bash
  yarn add @bowencool/eslint-config-vue -D
```

in `.eslintrc.js`

```js
module.exports = {
  extends: ['@bowencool', '@bowencool/vue'],
  rules: {
    // your rules
  },
};
```
